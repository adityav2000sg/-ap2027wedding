{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "set -e; echo \"=== boot: node $(node -v), PORT=${PORT:-unset} ===\"; npx prisma migrate deploy; SEED_IF_EMPTY=yes npm run db:seed; npm run accounts:provision; echo '=== starting next on port '\"${PORT:-3000}\"' ==='; exec npx next start -H 0.0.0.0 -p \"${PORT:-3000}\"",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
